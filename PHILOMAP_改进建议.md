# 脉络 Philomap — 改进 / 优化 / 新增 审计报告

> 审计日期：2026-07-17
> 审计对象：本地 `D:\applications\AI_files\philomap\index.html`（2102 行）+ `data/philosophers.json`（60 位哲人，1.05MB）
> 部署：https://yun-ai-base.github.io/philomap/
> 结论：项目已做过一轮性能优化（D3 自托管、力导向预计算 150 tick、星空动画按视图/可见性门控），基建扎实。以下为在此之上的进一步提升，**【已修复】** 标记项为本次已落地的代码改动。

---

## 一、已确认缺陷（Bug）

| # | 问题 | 证据 | 状态 |
|---|------|------|------|
| 1 | 时间轴图例 i18n 失效：JS 调 `getElementById('dp-tl-legend')`，但元素只有 class 没有 id → EN 模式下“生平/著作/世界事件”图例永远是中文 | 行 692（div 无 id）vs 行 1003（按 id 取） | ✅ 已修复（补 `id="dp-tl-legend"`） |
| 2 | 刷新后不应用已保存语言：`lang` 从 localStorage 读，但 `applyLang()` 只在点语言按钮时调用 → 选了 EN 刷新变回中文、按钮文案也不对 | 行 915 读 `lang`；`applyLang` 无启动调用 | ✅ 已修复（init 末尾调用 `applyLang()`） |
| 3 | 星图用 4 色区分文明，但界面无任何图例说明颜色含义 | CSS 文明色变量；无 legend 元素 | ✅ 已修复（右上角 `#civ-legend`，随语言切换） |
| 4 | 无深链接 / 分享：无法分享“某个哲人/某个视图”；测验“分享图片”无法回溯到结果 | 全文件无 URL 参数读取 | ✅ 已修复（`#view=…&p=…` + 详情面板“🔗 复制分享链接”按钮） |
| 5 | **孤儿关系 11 条**，指向数据集中不存在的 id：`epictetus / hanfeizi / wangyangming / lu-jiuyuan / whitehead / carnap / thomas-aquinas / john-stuart-mill` 等。在 `processData`/`renderGenealogy` 被静默丢弃 → “学术家谱/关联哲人”出现断链、数据不自洽 | 脚本扫描 143 条关系发现 11 条 dangling | ⚠️ 待处理（补哲人或清理关系） |
| 6 | `renderConceptNet` 每次开详情都 `new` 一个 forceSimulation 且不 stop 旧的，快速反复打开会累积后台模拟（轻微内存泄漏） | 行 ~1242 起 | ⚠️ 建议复用 / 关闭旧 sim |

---

## 二、体验 / 交互优化（中高价值）

1. **节点标签常驻且全显**（opacity .95），缩小时 60 个标签严重重叠。
   → 按缩放级别淡入标签；或仅显示高影响力 / 悬停 / 选中节点的标签。
2. **边未标注关系类型**：影响 / 师承 / 论战仅用 dash 区分，无 hover 提示（关系描述数据里有）。
   → 边 hover tooltip 显示「类型 + 描述」。
3. **键盘可达性弱**：仅 `f`（筛选）与 `Esc`（关闭）。
   → 加 `/` 聚焦搜索、方向键在节点间导航、数字键 1–6 切视图。
4. **图谱缩放后无小地图 / 概览导航**（节点多时容易“迷路”）。
5. **对比度 / a11y**：部分次级文本 `#64748b` 在深底上偏低；节点非键盘可聚焦；缺 ARIA。
6. **移动端真机走查**：详情面板全宽、底部时间条可能与底栏重叠，筛选为覆盖层——建议补一轮真机测试。

---

## 三、功能新增

1. **收藏 / 星标**（localStorage 持久化，跨会话）+ 筛选“仅看收藏”。
2. **随机哲人 / “惊喜发现”** 按钮，提升探索感。
3. **图谱内两两对比**：点击两个节点 → 触发“对决”或高亮共同概念。
4. **对决结果可分享**：当前对决结果只在弹窗内，可生成图片 / 带参链接。
5. **访问统计死代码**：行 2099–2100 仅 `localhost` 触发，线上是废逻辑（CountAPI 已移除）。
   → 要么部署隐私友好的本地计数并启用，要么删掉。
6. **孪生宇宙列表硬编码**（`TWIN_SITES` 写死在 index.html），新增项目需改代码重部署；而 psychscope 用 `projects.json` 拉取。
   → philomap 也改为拉取统一 `projects.json`，与孪生宇宙自动同步。

---

## 四、数据 / 内容扩充

- **文明圈严重失衡**：西方 45 / 东方 12 / 印度 2 / 伊斯兰 1。标榜“四大文明”但印度、伊斯兰几乎是点缀。
  → 补强：印度（商羯罗、龙树、陈那、泰戈尔）、伊斯兰黄金时代（法拉比、阿维森纳、阿威罗伊、安萨里、伊本·赫勒敦）、日本（京都学派）、非洲 / 拉美思想者。
- **scientificEval 仅 53/60**，缺 7 人。
- **头像仅 30/60**，另一半用首字母占位 → 可接公共领域画像或统一生成 SVG 字母牌。
- **测验仅中文**（无 EN 翻译），EN 用户使用受损。
- 时代划分可细化（启蒙法/德分流、分析哲学、存在主义等）。

---

## 五、工程 / 技术债

1. **单文件 2102 行**（CSS+HTML+JS 混写）。可拆 ES module（`data.js` / `graph.js` / `views/*`），静态托管用 `type="module"` 仍可用，利于维护。
2. **维度打分是关键词匹配（启发式）**：对决 / 光谱的分数界面应标注“示意性 / 非权威”，避免被误读为严谨结论。
3. **1MB JSON 全量 fetch**；若扩到数百哲人，建议按文明分片懒加载或压缩。
4. **缺自动化冒烟测试**；建议加 Playwright（加载、切视图、开详情无报错）。

---

## 六、本次已落地改动清单（index.html）

- 行 692：`dp-tl-legend` 补 `id`（修复 EN 图例）。
- 行 604：新增 `#civ-legend` 容器；CSS 新增其样式（桌面显示、移动端隐藏）。
- 行 1003 区：`applyLang()` 内填充文明圈图例（随语言切换）。
- 启动流程（init 末尾）：调用 `applyLang()`（修复刷新不应用语言）+ 解析 URL hash 应用初始视图/哲人。
- 新增深链接模块：`readHash / syncHash / hashchange` 监听；`switchView / openDetailPanel / closeDetailPanel` 内同步 hash。
- 详情面板：新增 `🔗` 复制分享链接按钮（含 clipboard 兜底）。
- 语法校验：`node --check` 通过。

> 注：以上仅改本地文件。要上线需重新部署（GitHub Pages，走 Contents API 上传或本机 git push 到 master）。未做数据改动（孤儿关系 / 文明圈补强留待你确认）。
